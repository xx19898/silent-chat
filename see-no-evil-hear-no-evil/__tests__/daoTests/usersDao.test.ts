/*
import { usersPlainDAO } from '@/data-access/chatUsers'
import { expect, test, describe } from 'vitest'

describe('Testing data manipulation functions of the DAO object', async () => {
    test('', async () => {
        const users = await usersPlainDAO.getUsers()

        expect(users.length).toBe(3)

        await usersPlainDAO.createNewUser('new user')

        const updatedUsers = await usersPlainDAO.getUsers()
        expect(updatedUsers.length).toBe(4)
    })
})
*/
